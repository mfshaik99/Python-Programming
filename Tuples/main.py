# Tuple in Python
coordinates = (1, 2, 3)

# Accessing elements by index
print(f"First coordinate: {coordinates[0]}")  # 1

# Tuple unpacking
x, y, z = coordinates
print(f"Unpacked values: x={x}, y={y}, z={z}")
